version: "3.9"

services:
  backend:
    image: haechanlovelove/backend:latest
    ports:
      - "8000:8000"
    restart: always

  frontend:
    image: haechanlovelove/frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
